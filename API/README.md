## to setup in your computer (if you don't wanna use docker)
cd to this folder (ex: cd ShareSplit/API)
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
flask run

## to run after initial setup (pls don't do initial setup each time)
cd to this folder (ex: cd ShareSplit/API)
venv\Scripts\activate
flask run